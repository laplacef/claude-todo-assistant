End-of-day wrap-up — close out the day cleanly.

1. Check `scratchpad.md` for any unprocessed notes. If there are any, process them now (update `tasks.md` and file accordingly).
2. Check `meetings/` for any unprocessed meeting notes from today.
3. Do a final update to `tasks.md` — mark completed items, add anything new, flag blockers.
4. Update today's daily note in `daily-notes/` (format: `YYYY-MM-DD.md`) with an end-of-day summary: what got done, what's still open, what's blocked.
5. Write any important context, follow-ups, or things to remember to `.claude/memory.md` so it carries over to tomorrow.
6. Commit all changes with a descriptive message summarizing the day's updates. Use standard git commit (do not use --no-gpg-sign or skip any git configuration).
7. Give me a brief end-of-day summary — nothing should fall through the cracks.
